# LPR 서버모드 멀티카메라 — 빌드/작업 로그

> 매 빌드마다 이 파일을 갱신한다. (Claude 재실행/PC 재부팅 후 이 파일을 먼저 읽고 이어서 작업)
> 빌드 명령: `"C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" "E:\LPRCAM\KukjeLPR\KyungsinLPRx64.csproj" -p:Configuration=Debug -p:Platform=x64`
> 출력: `E:\LPRCAM\KukjeLPR\bin\x64\Debug\KyungsinLPRx64.exe`  (빌드 전 실행중 프로세스 KyungsinLPRx64 종료 필요)

---
## 기능 개요
GPU 없는 LPR PC가 캡처 이미지를 **ParkingWeb 서버로 보내 인식 위임**, 게이트/전광판/정기권은 LPR이 처리.
화면은 **WinForms 네이티브 카드 그리드(최대 15대=가로5×세로3)**. 비서버모드(기존 2카메라)는 그대로.

## INI 키 (CameraSetting.ini)
- `[OPTIONK] servermode` = 멀티카메라 카드 화면 ON/OFF (동작모드 '서버모드' 라디오)
- `[OPTIONK] remote` = 원격 차번인식(ParkingWeb 위임) ON/OFF (업로드 그룹 '원격 차번인식 사용' 체크박스). **servermode와 별개**
- `[OPTIONK] remote_noupload` = 원격 차번인식만 사용(이미지 업로드 안함). remote 체크 시에만 선택가능. true면 clsImageUploader.Enabled=false(인식은 OptionK 그대로, 업로드만 끔). 서버+LPR 같은 PC 공존 시 이미지 중복저장 방지
- `[OPTIONK] camcount` = 사용 카메라 대수(1~15)
- `[OPTIONK] serverurl/apikey` = 비면 [UPLOAD] 값 재사용
- `[SVRCAM1]~[SVRCAM15]` = 카메라별 개별설정. `name`(카드 표시명), `percam_configured`, `pc_<컨트롤명>`(개별설정 직렬화)

## 핵심 파일
- `Class/RegModule/ServerCamConfig.cs` — 서버캠 설정 모델([SVRCAM] 로드/저장), MAX=15/COLS=5/ROWS=3
- `Class/Controls/CameraCard.cs` — 카드 UserControl(헤더+영상+인식결과+태그+캡처버튼+최근이력). 더블클릭=CardDoubleClicked
- `Class/Controls/ServerModePanel.cs` — 제목+5×3 TableLayout+시계+환경설정버튼(EnvClicked)
- `Class/RegModule/OptionK.cs` — 인식 클라이언트. remote=true면 ParkingWeb `POST /api/lpr/recognize`(multipart file/roi/apiKey)→{ok,plate,conf,bbox}
- `Form/frmLprMain.cs` — InitServerModeUI(서버모드면 카드패널 Dock=Fill), SetBitmap→MirrorToServerCard(cam1→카드0,cam2→카드1), UpdateServerCard(차번표시), 카드 캡처→btnCam1/2Capture_Click, 더블클릭→frmEnv per-camera, 폼크기 자동(BeginInvoke), Alt+S TEST 버튼 BringToFront
- `Form/frmEnv.cs` — 서버모드 환경설정 필터 ApplyServerModeUi(공통/개별 반대), SetServerCamMode(개별), SaveServerCam/LoadServerCam/WalkPerCam([SVRCAM] 직렬화), btnSetROI(개별시 _serverCamIndex로 카메라판정)
- `Form/frmServerCams.cs` — (구) 서버캠 그리드. 현재 버튼 숨김(btnServerCams.Visible=false), 미사용

## 환경설정 필터 (서버모드 시)
- 공통모드(환경설정 버튼): 활성=DB·동작모드·긴급차량·인식률보정·정기권취득방식·현재주차대수·소켓통신·블랙리스트·인식모듈·이미지저장 / 비활성=카메라설정·차단기·전광판 탭, 만차·부제·동일차량·주차장, LPR통신/장비/인증, 입출차연동(groupBox16), 차단기그룹제한
- 개별모드(카드 더블클릭, 반대): 활성=카메라설정·차단기·전광판·입출차연동·차단기그룹제한 / 추가비활성=1·2번카메라버튼(btnCam1/2)·usbCamPanel, 전광판2(Display2), LPR2(groupBox12), 차단기 출력2(Gate2)
- 개별 저장은 [SVRCAM{n}]에만(전역 미변경), 창 유지(닫기 버튼으로 종료)

## 현재 동작/한계
- ✅ cam1/cam2(iNova 1,2): 카드 영상 미러링 + 캡처 → ParkingWeb 인식 → 카드 차번표시 + 차단기/전광판/입차 처리 (실측 790두1468, 98.2%, 151ms)
- ✅ 카드 3~15(인덱스2~14): A(영상)+B(인식)+**C(정산: 게이트·입출차·정기권)**+**D(카메라별 네트워크 전광판)**. 게이트=공유 Dingtian 릴레이 '포트번호'로 카메라 구분
- DataProcess/GateOpen은 CamIdx>=2 일반화 완료. 0/1 동작 불변
- ⚠️ 카메라별 게이트 포트·전광판 IP는 서로 달라야 함(현재 INI는 전부 동일값=복사됨 → 카드 더블클릭 개별설정 필요)

## 다음 작업 (3~15대 실연동, 단계별)
- ✅ **A단계(완료)**: `Class/Controls/ServerCamManager.cs` — 카드3~15(인덱스2~14) iNova2 연결+그랩스레드→카드 영상표시. IP=[SVRCAM{n}] pc_txtCamIp(개별설정 카메라설정 IP). cam1/2(인덱스0,1)는 기존 GrabLoop가 미러링. frmLprMain._serverCamMgr가 InitServerModeUI에서 Start, Rebuild/재진입시 Dispose. **A단계는 영상만**(캡처/인식 없음)
- **B단계(다음)**: 카드3~15 캡처(버튼/DIO)→이미지저장→ParkingWeb 인식(RecognizeRemote)→카드 차번표시. 기존 RegArray/OptionK.Reg는 camIdx 0/1뿐이라, 추가카메라는 ServerCamManager에서 직접 캡처+RecognizeRemote 호출하는 별도 경로 필요(또는 C단계 RegArray 확장 선행).
- ✅ **C단계(완료)**: 배열[15] 확장 + clsServerCamEnv([SVRCAM] 장비/게이트) + DataProcess/GateOpen camIdx 일반화 + ProcessServerCamResult. 카드3~15가 캡처→인식→게이트개방·입출차기록·정기권 정산. (AfterRegPlateCam은 0/1 전용 유지, 서버캠은 ProcessServerCamResult→DataProcess 직접 경로)
- **D단계(후속, 선택)**: 카메라별 네트워크 전광판 출력([SVRCAM] DispIp/DispPort). 현재 서버캠 전광판 무출력

---
## 실제 설정 검증 (2026-06-19, camcount=5, servermode=True)
- ✅ 저장 정상: SVRCAM1~5 각 `pc_txtEqpmNo1`(1~5)·`pc_txtLPRNo1`(CH01~05)·`pc_CmbLPRInOut1`(입구/출구) 기록됨
- ✅ 카메라 IP 분리: cam1=217, cam2=216, SVR3=211, SVR4=214, SVR5=212 (충돌 없음)
- ✅ Dingtian 공유박스 .110 (모든 [SVRCAM] pc_txtDioIp=192.168.1.110, [COMMON] dioip=110)
- ❗ **게이트 포트 충돌**: 모든 카메라 `pc_CmbGate1Port=1` + [COMMON] gate1port=1/gate2port=2. 게이트는 Dingtian 릴레이 '포트번호'로 구분 → **5대가 전부 포트1이면 같은 차단기만 열림**. 카메라별 다른 포트 필요(예: cam1=1, cam2=2, cam3=3, cam4=4, cam5=5). 카드 더블클릭→차단기설정→출력PORT 변경
- ⚠️ **8릴레이 한계**: Dingtian 8채널 → 게이트 동시 최대 8대. 9~15대 게이트는 2번째 Dingtian 박스(다른 DioIp) 필요 = 향후작업(현재 단일 공유박스만 지원)
- ⚠️ 전광판: 서버캠 미적용(D단계). DisPlay.Use=false

## 빌드 이력 (최신이 위)
- **2026-06-24 15:18 카메라1·2 전광판이 전역 유령IP(.115)로 송신하던 문제 수정**
  - 증상: 서버모드에서 카메라1 전광판이 [전광판송신] 카메라1 192.168.1.115:5000 으로 송신. 5개 카메라 어디에도 .115 설정 없음
  - 원인: 전역 [전광판] display1netip=192.168.1.115(옛 값)가 살아있음. 카메라1(레거시 NetDisPlay1)은 [SVRCAM1] 네트워크전광판 사용=0이라 개별값이 없어 ApplyCardDisplay 가 전역값 유지 → NetDisPlay1 이 .115 로 연결·welcome 송신. 서버모드는 전역 전광판 탭 숨김이라 유령값
  - 수정: ApplyCardDisplay — 서버모드에서 개별 네트워크전광판이 꺼져 있으면(pc_chkDisplay1NetUse!=1) DisPlay[idx].Use/Net.Use=false 로 **그 카메라 전광판 끔**(전역 .115 송신 금지). 켜져 있으면 기존대로 개별 IP/문구 주입. 로그 `카드 전광판 미사용 — 전역값 무시`
  - 비고: 카메라5 환영문구 '안세요/어세요'는 사용자 테스트 입력(손상 아님). 배치저장 한글보존 검증 정상
- **2026-06-24 15:12 전광판 Test 버튼 '깨짐' 수정(런타임 전광판 단일소켓 재사용)**
  - 증상: 카메라5 개별설정에서 일반차량/정기차량 Test 누르면 전광판 깨짐(문자 깨짐). 모든 SVRCAM 전광판 IP가 동일 .201, 카메라5만 사용=1
  - 원인: 14:57 선연결로 카메라5 런타임 전광판(_svrNetDisp[4])이 .201 에 연결돼 welcome 루프로 매초 송신 중. 15:05 Test 의 SendDisplayTest 가 **별도 2번째 소켓**(_testDisp)으로 같은 .201 에 동시 송신 → 두 소켓 바이트 섞임 → 깨짐
  - 수정: `SendDisplayTest` 가 `GetRuntimeDisplayForTest(_serverCamIndex)`로 **같은 보드 런타임 인스턴스(단일 소켓) 재사용** + `DisPlayTime=now`로 welcome 루프 Term 초 비켜섬 → 충돌 제거. IP 다르거나 런타임 없으면 기존 임시연결 폴백. (cam0/1=NetDisPlay1/2, cam2~=NetDevForServer). 빌드 15:12
- **2026-06-24 15:05 전광판 설정 'Test' 버튼이 화면 입력 IP로 송신(개별설정/서버모드 대응)**
  - 증상: 전광판 설정 탭의 기본문구/일반차량/정기차량 Test 버튼이 동작 안 함(특히 서버모드 개별설정)
  - 🐞 원인: btnDisplay1/2Test*_Click 6개가 모두 **전역 `frmLprMain.NetDisPlay1/2`**(시작 시 연결된 카메라1/2 장치)로 송신 + 게이트가 `env.DisPlay[0/1].Net.Use`. 개별설정(카메라3 등)에서 눌러도 카메라1 보드로 가거나 시리얼 분기로 빠져 무동작. 화면에 입력한 IP(.201)를 안 씀
  - 수정: `frmEnv.SendDisplayTest(ip,port,...)` 추가 — **화면 입력 txtDisplayNNetIp/Port 로 직접 연결·송신**(같은 대상 재사용, 비동기 연결 300ms 대기). 6개 Test 분기의 네트워크 경로를 `chkDisplayNNetUse.Checked` 게이트 + SendDisplayTest 로 교체. 서버모드 개별설정에서도 그 카메라 보드로 정확히 테스트. 빌드 15:05
- **2026-06-24 15:01 서버캠 전광판 2열 차번 우측정렬 수정**
  - 증상: 카메라3 전광판 표시되나 2열(차번)이 좌측정렬(카메라1·2는 우측정렬)
  - 원인: ServerCamDisplay 가 plate 를 패딩 없이 송신. 카메라1·2(레거시)는 `CarNoSpace(CarNo)`(12바이트 폭 좌측 공백 패딩=우측정렬)를 사용
  - 수정: `frmLprMain.PlateRightAlign(carno)` 추가(CarNoSpace 동일 로직: CP949 바이트수 기준 12폭 좌측패딩) → ServerCamDisplay 의 2열 인자에 적용. 빌드 15:01
- **2026-06-24 14:57 서버캠(카메라3~15) 전광판 시작 시 선연결 — '카메라3 전송 안 함' 대응**
  - 증상: 카메라1·2는 전광판 정상인데 카메라3 설정 후 테스트하면 전송 안 됨([SVRCAM3] pc_chkDisplay1NetUse=1, IP=.201 정상 저장 확인)
  - 분석: 카메라1·2는 NetDisPlay1/2 를 **시작 시 연결**(frmCamMain_Load 1342). 서버캠은 NetDevForServer 가 **첫 인식 때 지연 생성** → TCP 연결 완료 전 첫 SendMsg 가 누락 가능(연결은 async). 빌드로그 기존 경고 "전광판 캐시 → 재시작 후 반영"과 동일 맥락
  - 수정: `frmLprMain.InitServerCamDisplays()` — 서버모드 시작 시(clsServerCamEnv.Load 직후) i=2..camcount-1 에 대해 ApplyCam(i)+NetDevForServer(i) 호출로 **선연결+welcome 루프 시작**. 로그 `[서버캠N] 전광판 시작연결 완료/미설정`
  - 진단: 재시작 후 시작로그에 `[서버캠3] 전광판 시작연결 완료` + 테스트 시 `[전광판송신] 카메라3 192.168.1.201:5000 ...` 가 뜨면 송신 정상. 그래도 보드 무표시면 보드 연결/공유IP 문제로 좁혀짐(카메라1과 같은 .201 공유 시 보드가 2연결 거부 가능). 빌드 14:57
- **2026-06-24 14:45 카메라 개별설정 저장 속도 개선(203키 개별쓰기 → 1회 배치)**
  - 증상: 서버모드 카메라 개별설정 저장 시 수 초 지연
  - 원인: `SaveServerCam`의 `WalkPerCam(save)`가 컨트롤마다 `Util.Function.IniWriteValue` 호출 = [SVRCAM]당 **203키**. 그 함수는 DLL `WritePrivateProfileString` 기반이라 **호출마다 39KB INI 전체 재파싱·디스크 플러시** → 203회 누적 = 수 초
  - 수정: `WalkPerCamCollect`(값만 dict 수집) + `IniWriteSectionBatch`(INI **1회 읽기+1회 쓰기**로 [SVRCAM{n}] 섹션 일괄 갱신). 기존 키 제자리 갱신, 신규 키 섹션 끝 추가. 인코딩 `Encoding.Default`(CP949)=WritePrivateProfileString ANSI와 동일 → 한글 보존(라운드트립 검증 완료). 경로=`Util.Function.IniFileName`. 실패 시 기존 개별쓰기로 폴백
  - 효과: 저장 203회 디스크 기록 → 2회. 체감 수 초 → 즉시. 빌드 14:45
- **2026-06-24 14:39 카메라1·2 전광판 '문구/색상'도 개별설정 적용(전역값 무시) — 환영문구 버그 수정**
  - 증상: 카메라1 개별설정에 전광판 문구 1번=안녕/어서 저장했는데 보드엔 전역값 '안녕하세요/어서오세요'(display1ment1line) 송출
  - 🐞 원인: 14:19 의 ApplyCardDisplay 가 **IP/포트만** 개별값 주입, 문구/색상은 전역 DisPlay[0].Ment 그대로 → cam0/1 레거시 경로가 전역 환영문구 송출
  - 수정: ApplyCardDisplay 가 [SVRCAM] **1번 필드**(pc_txtDisplay1Text1/2, pc_CmbDisplayText1Color1/2, pc_txtNormalCar1, pc_CmbDisplayTextNormal1Color1/2, pc_txtPeriodCar1, pc_CmbDisplayTextPeriod1Color1/2, pc_CmbDisplay1Type)를 DisPlay[idx] 에 주입. 서버모드는 카메라 1대=전광판 1대라 **무조건 1번 설정만 사용**(2번 미사용). 로그에 환영/일반/정기 문구 표시. 빌드 14:39
- **2026-06-24 14:29 카메라별 전광판 송신 로그 추가(어느 카드가 어느 보드에 무엇을)**
  - `NetworkDisplay`에 `Tag`(카드1/카드2/카드3…) 필드 + `SendMsg` 진입 시 로그: `[전광판송신] 카드N IP:Port 1열='문구'(색) 2열='문구'(색)`
  - 중복 억제: 직전 송신과 내용 동일하면 생략(`_lastLogSig`) — NormalDisPlay 환영문구 매초 재전송 스팸 방지. 내용 바뀔 때만 1줄
  - 태그 지정: NetDisPlay1="카드1", NetDisPlay2="카드2"(frmLprMain 전광판 초기화), 서버캠 NetDevForServer="카드"+(idx+1)
  - 모든 송신 경로 공통 적용(인식 차번/주차요금/환영문구/만차). 빌드: 14:29
- **2026-06-24 14:19 서버모드 카드1·2(인덱스0,1) 네트워크 전광판 개별설정 미적용 버그 수정**
  - 증상: 서버모드에서 카드1 전광판을 192.168.1.201:5000으로 저장해도 무출력. INI엔 [SVRCAM1] pc_txtDisplay1NetIp=192.168.1.201 정상 저장됨
  - 🐞 원인: `clsServerCamEnv.Load`의 `for(i=2; i<camCount)` 가 **인덱스 0,1 건너뜀** + `ServerCamDisplay`/`NetDevForServer` 가 `camIdx<2 return`. 그래서 카드1·2의 [SVRCAM1/2] 전광판 설정이 아무도 안 읽음 → cam0/1 은 옛 전역 [전광판] IP(.115)를 그대로 사용. 서버모드는 공통 전광판 탭이 비활성이라 .201 설정 경로가 카드 개별설정뿐인데 그게 무시됨
  - 수정: `clsServerCamEnv.ApplyCardDisplay(idx)` 신규 — 서버모드 시 카드1·2의 전역 `DisPlay[0]/[1].Net`(Use/IP/Port)+Use/UseFiex 를 [SVRCAM1/2] pc_chkDisplay1NetUse/pc_txtDisplay1NetIp/Port 로 덮어씀. **전광판 한정**(게이트/인식/정산 무영향). Load() 끝에서 `if(ServerMode){ApplyCardDisplay(0);ApplyCardDisplay(1);}` 호출 — NetDisPlay1/2 초기화(frmCamMain_Load 1342)보다 먼저라 초기화가 개별 IP 사용. 개별 네트워크 전광판 미설정([SVRCAM] pc_chkDisplay1NetUse!=1)이면 전역값 유지
  - cam3~15(인덱스2~14)는 기존 ServerCamDisplay 경로 정상 — 단 [SVRCAM3~]의 pc_txtDisplay1NetIp 가 현재 전부 .115 동일이라 카메라별 실보드 IP로 분리 필요(코드 아닌 설정문제)
  - ⚠️ 재시작 후 반영(전광판 캐시). 빌드: KyungsinLPRx64.exe 2026-06-24 14:19

- **2026-06-19 16:12 무인정산기 LPR접속실패 원인 = 고아 사이드카 포트점유. 수정**
  - 원인: 어제 비원격 세션이 띄운 Python 사이드카(optionk_server.py)가 부모 LPR의 리스닝 소켓(요금계산기 40000·입차 30000)을 **상속**. 부모 LPR 종료 후에도 사이드카가 살아 포트 점유 → 새 LPR이 바인딩 실패(무음) → 무인이 LPR 접속 불가. (소켓 설정은 calcuse=True로 정상)
  - 즉시조치: 고아 사이드카(PID 8100) 종료 → 포트 해제
  - 코드: ① SocketInit의 LprExitSvr/LprEntSvr StartServer를 try/catch로 감싸 **바인딩 실패 시 명확히 로그**("...서버 시작 실패 포트 40000 — 중복실행/포트점유 확인! 무인정산기 접속 불가") ② `OptionK.KillStaleSidecars()`(WMI로 optionk_server.py python 종료) 추가, **원격모드 Initialize에서 호출**(SocketInit 전이라 점유 선해소)
- **2026-06-19 15:45 개별 입출차 처리옵션(LprOpt) 미적용 버그 수정**
  - 🐞 개별설정의 입출차 옵션(차단기/소켓/LprTrns 등)이 [SVRCAM]에 `pc_chkNCarEntGate`·`pc_chkNCarExitGate` 등으로 저장되나, clsServerCamEnv가 **안 읽고 cam1(LPR1) 글로벌만 상속**시켜 미적용
  - 수정: `clsServerCamEnv.TryGetLprOpt(idx, inOut)` — 입구면 Ent섹션, 출구면 Exit섹션 pc_chk* → ProcessOption. ApplyCam이 서버캠 LprOpt에 적용. DataProcess는 서버모드 시 **모든 카메라(cam0/1 포함)** LprInfo.LprOpt를 개별값으로 덮어씀(percam_configured 아니면 글로벌 유지). 기존 cam2→LPR1 하드코딩 제거
  - (16:00 보정) 처리옵션은 **방향 무관 LPR1(입차/Ent) 섹션 하나만** 사용. 방향(입구/출구)은 LPR 입출구분(CmbLPRInOut1)으로 별도 결정. TryGetLprOpt pfx="Ent" 고정 → 출구 카메라도 입차 옵션 1개만 설정하면 됨
- **2026-06-19 15:15 Dingtian 입력 폴링 주기 INI 설정화 + 기본 100→50ms(검지 반응 향상)**
  - clsSerialPort: `StartInputPolling(100)` → `[COMMON] diopollms`(없거나 범위밖 30~5000이면 50ms). 로그에 "(입력폴링 Xms)" 표시
  - ClsDingtian 최소 클램프 50→30ms. 검지 지연 ~100ms대→~50ms대로 단축. 단일스레드 순차폴링이라 부하·파일업 없음
- **2026-06-19 14:49 15대 풀시나리오 분석 — 동시성 버그 수정 + 다중 Dingtian 박스 지원**
  - 🐞 동시성(TCon 경쟁): cam0/1(AfterRegPlateCam, lock없음)+서버캠이 단일 DB연결(TCon)·NetDev를 동시사용 → SQL예외/데이터깨짐(15대 동시캡처 시 빈발). `clsDataTransaction.ProcLock`(static) 공유락으로 **모든 DataProcess 호출 직렬화**(AfterRegPlateCam·ProcessServerCamResult). _serverProcLock 대체
  - 다중 Dingtian 박스: 1박스=8릴레이라 15게이트 불가. `clsSerialPort.DingtianFor(idx)`가 카메라별 DioIp([SVRCAM] pc_txtDioIp)가 메인과 다르면 그 박스로 라우팅(IP별 캐시·생성). clsServerCamEnv._dioIp/GetDioIp 추가
  - ⚠️ 설정요건(15대): 게이트포트 박스당 1~8 + 카메라별 Dingtian IP 분산, 전광판 IP 카메라별 상이, 카메라 IP 상이
- **2026-06-19 14:33 정기차량 일반표시 버그 수정 + cam2 게이트(설정) 진단**
  - 🐞 정기→일반 오표시: `DataProcess` finally(1081)에서 `RegedCar[CamIdx]=false` 무조건 리셋 → 직후 `GetRegedCar` 항상 false. 리셋 직전 `_lastReged[CamIdx]=RegedCar[CamIdx]` 보존, GetRegedCar가 `_lastReged` 반환. (데이터는 처리중 true라 정기 정상저장, 표시만 틀렸던 것)
  - cam2 게이트(코드 수정): 서버모드인데 cam2(CamIdx=1)가 글로벌 **Lpr2Info 처리옵션**(차단기 OFF)을 봐서 안 열림. 개별설정은 LPR1 컨트롤로 저장되므로, **서버모드면 모든 카메라의 LprOpt를 LPR1 기준으로 통일**(`clsServerCamEnv.ServerMode && CamIdx==1 → LprInfo.LprOpt=Lpr1Info.LprOpt`). InOutType/채널/게이트포트는 각 카메라 유지. (앞서 INI 우회는 원복)
  - 14:16 차번색 일반/정기 구분(흰색/분홍)도 포함
- **2026-06-19 14:09 개별설정 영역설정(ROI)·카메라설정 서버캠 지원 + 카메라설정 크래시 수정**
  - 🐞 btnCamSetup_Click: perCam에서 groupBox1.Text 불일치→frm=null→ShowDialog NullRef 크래시. null가드+try/catch, 서버캠은 라이브 디바이스(_serverCamDev)로 iNova2.frmAdvFeature, 0/1은 _serverCamIndex 보완
  - 영역설정: 서버캠(>=2)은 카드 스냅샷 이미지로 frmPicConfig(신규 생성자 env,imagePath,roi) 열어 ROI 설정→[SVRCAM{n}] roi/pc_roi 저장. ServerCamManager.CaptureWork가 roi 읽어 RecognizeImage에 전달(0,0,0,0/빈값=전체)
  - frmLprMain 카드더블클릭: `_serverCamMgr.GetDevice(ci)` + 카드 스냅샷 임시저장 경로를 frm.SetServerCamDevice로 전달. ServerCamManager.GetDevice 추가
- **2026-06-19 13:52 카드 입출구/차량종류 태그 동적표시 + 최근이력 삭제 + 전광판 바이트로그 제외**
  - 입출구 태그: 카메라별 InOutType([SVRCAM] pc_CmbLPRInOut1)로 입구=입차(녹색)/출구=출차(적색). `CameraCard.SetGate(isExit)`, InitServerModeUI에서 카드별 설정
  - 차량종류 태그: 인식 시 정기=분홍/일반=회색/미인식=숨김. `CameraCard.SetCarType(0/1/2)`, `frmLprMain.UpdateServerCardType`. 서버캠=ProcessServerCamResult, cam0/1=clsThread(DataProcess 후 GetRegedCar)
  - 최근 이력 버튼 삭제(+카드 높이 274→248, 폼 행높이 292→266)
  - NetworkDisplay 전광판 전송 바이트 hex 덤프 로그 제거(노이즈)
- **2026-06-19 13:39 차단기 열기 지연 수정(Dingtian UDP 전용 보드 대응)**
  - 현장로그: 차단기 개방마다 "DINGTIAN TCP 실패 → UDP 폴백" = 매번 TCP 1.5s 타임아웃 후 UDP. 이 보드는 릴레이 TCP(60001) 미지원·UDP만 받음(그래서 폴백으로 열림). 인식은 빠른데 차단기만 1.5s 지연
  - 원인: 직전(12:29) '영구 UDP 래치 제거' 변경이 이 보드엔 역효과(매 차단기 TCP 재시도). 실제 "안 열림"은 포트0(NRE) 버그였고 그건 이미 수정됨
  - 수정: `ClsDingtian.RelayOn` — TCP 실패 시 `PreferUdp` 전환(이후 차단기 즉시 UDP, 지연 없음) + 30회마다 TCP 재시도(복구감지). TCP 연결 타임아웃 1500→600ms(첫 1회만 부담). 결과: 첫 차단기 ~0.6s, 이후 즉시
- **2026-06-19 13:28 전광판(D단계) — 카메라별 네트워크 전광판 출력**
  - DataProcess 전광판 경로(운영중 cam1/2)는 **건드리지 않고**(서버캠은 NetDev=null로 표시 안 함), 서버캠 전용 출력을 ProcessServerCamResult 뒤에 추가 = cam1/2 무위험
  - `clsServerCamEnv.ApplyCam`: DisPlay[idx]에 [SVRCAM] 전광판설정 주입(Net.IP/Port, 환영문구/색상, 정기/일반 문구·색상). 직렬은 Use=false(오출력 방지)
  - `frmLprMain.NetDevForServer(idx)`: 카메라별 NetworkDisplay 생성·캐시(환영문구 ReturnStart 루프) / `ServerCamDisplay(idx,plate,reged)`: 정기='정기차량'+차번, 일반='일반차량'+차번, 미인식=환영문구 유지. Term초 후 환영문구 복귀
  - `clsDataTransaction.GetRegedCar(idx)`: 직전 정기권 여부 조회(전광판 표시용). ProcessServerCamResult가 lock 안에서 읽음
  - ⚠️ 전광판 IP 카메라별로 달라야 함(현재 전부 .115 동일). 설정변경은 재시작 후 반영(전광판 캐시)
- **2026-06-19 12:47 코드분석 버그 2건 수정 + 원격인식 체크박스 디자이너 이전**
  - 🐞 DataProcess 소켓 ChName: `CamIdx==0?IPCamera1:IPCamera2` → 서버캠(>=2)이 무조건 CH02로 전송되던 버그. `camChName`(서버캠=LprInfo.ChNo)·`camStxEtx` 변수 도입해 4곳 ChName+3곳 STXETX 교체
  - 🐞 DataProcess DB기록 Lpr: `CamIdx.Equals(0)?Lpr1Info:Lpr2Info` 8곳 → 서버캠이 Lpr2Info로 기록되던 버그. 이미 올바른 `LprInfo`(GetLpr)로 전부 교체(0/1 동일, >=2 교정)
  - 🖥️ 원격 차번인식 사용/이미지업로드안함 체크박스: 코드생성→**Designer(gbUpload) 정식 컨트롤로 이전**(VS 디자이너에 표시), gbUpload 165→210 확대(잘림 해소). 연동(활성조건)만 코드 유지. frmEnv.cs 중복필드 제거
  - ⏳ 전광판(D단계): 인프라 조사만 함(NetworkDisplay/DisPlay 매핑). 코드변경 없음 — 다음 진행
- **2026-06-19 12:36 Alt+S 상단 TEST1/2 숨김(서버모드) + 카드 하단 잘림 수정**
  - 서버모드 Alt+S: 상단 btnTestCapture1/2·txtTestCarNo 미표시(카드 자체 TEST 있음). 일반모드는 기존대로
  - 카드 잘림: Dock 콘텐츠 266px > 카드높이 234 → 하단(최근이력) 잘림. 카드 274로, 폼 행높이 250→292
  - ⏳ 사진 받고 세부 조정(영상크기/글자/간격) 예정
- **2026-06-19 12:34 OptionK 로컬 사이드카 실행 조건 정리**
  - 원격 차번인식(remote=True): 로컬 파이썬 사이드카 미실행(진행바/스레드 없이 OptionK.Initialize만 — 내부적으로 원격 설정만 즉시 준비). "사이드카 시작" 로그/진행바 안 뜸
  - 로컬 사이드카는 **카메라서버+자료처리(StartType==BOTH) + 원격아님**일 때만 시작. 그 외(COM/CAM)는 미실행 로그
  - frmLprMain 시작 시퀀스, RegModule==OptionK 블록
- **2026-06-19 12:29 서버캠 차단기 안 열림 2건 수정(NRE 타이밍 + Dingtian 콜드스타트)**
  - ❗NRE: `clsServerCamEnv.Load`가 `ENV=func.GetEnv(ENV)`(frmCamMain_Load 1214줄)보다 **먼저** 실행돼 ENV 배열 null→NRE. 게다가 ENV가 새 구조체로 재대입돼 주입이 날아감. 결과 DioOutPut[2~4].Port=0 → `RelayOn(0)`은 `port<1 return`이라 무동작
    - 수정: `clsServerCamEnv` 재작성 — `ApplyCam(idx)`(현재 ENV에 카메라별 게이트포트/장비 멱등 주입, 널안전) + `Load()`(camcount 자동). 호출을 ENV 로드 직후(frmCamMain_Load)와 **캡처 직전(ProcessServerCamResult)** 에 수행 → ENV 재대입에도 항상 올바른 포트 보장. InitServerModeUI의 이른 호출 제거
  - ❗Dingtian 콜드스타트: 첫 RelayOn TCP 연결이 500ms 안에 안 되면 타임아웃→`PreferUdp=true` **영구 래치**→이후 전부 UDP(보드 TCP만 받으면 무동작). "첫 인식 차단기 안 열리다 릴레이 테스트 후 정상" 증상
    - 수정: `ClsDingtian` 영구 UDP 래치 제거(실패해도 다음 호출 TCP 재시도) + TCP 연결 타임아웃 500→1500ms
- **2026-06-19 12:22 '원격 차번인식만 사용(이미지 업로드 안함)' 옵션 추가**
  - 사유: 현장에서 서버+LPR 같은 PC 공존 시 LPR이 로컬 이미지저장 + 이미지서버 업로드로 같은 이미지 중복저장 문제. 인식만 원격 쓰고 업로드는 끄고 싶음
  - `frmEnv`: 소켓통신>이미지업로드(gbUpload), '원격 차번인식 사용'(chkOcrRemote) 바로 아래 `chkOcrRemoteNoUpload` 추가. **chkOcrRemote 체크 시에만 Enabled**(CheckedChanged 연동, 해제 시 자동 uncheck). 저장 [OPTIONK] remote_noupload, 로드 시 enabled/checked 복원
  - `clsImageUploader.Start()`: [OPTIONK] remote_noupload=true면 Enabled=false(업로드 무력화, 인식 OptionK는 무관). 저장 후 remote_noupload 기록 뒤 Reload 한번 더(SaveIni Reload는 기록 전이라)
- **2026-06-19 12:03 Alt+S 시 카드 캡처버튼 자리에 [차번입력][TEST] 토글 추가**
  - `CameraCard`: `_testPanel`(차번 TextBox + TEST 버튼) 추가, `SetTestMode(bool)`로 캡처버튼↔테스트 토글. 이벤트 `TestClicked`(클릭)·`TestPhotoClicked`(우클릭), 속성 `TestPlate`. 차번창 Enter도 TEST 실행
  - `ServerModePanel.SetTestMode(on)`: 전체 카드 토글
  - `frmLprMain`: Alt+S KeyDown에서 `_serverPanel.SetTestMode(true)`, 60초 후 timeCheck에서 false 복귀. 카드 `TestClicked→OnCardTest(ci,plate)`, `TestPhotoClicked→OnCardTestPhoto(ci)`
  - `OnCardTest`: ci 0/1=기존 btnTestCapture1/2_Click 재사용(txtTestCarNo 세팅), ci>=2=`ProcessServerCamResult(ci,plate,null,now)`(이미지 없이 차번 수동정산)
  - `OnCardTestPhoto`: ci 0/1=기존 BtnTestCapture1/2_MouseUp(우클릭) 재사용, ci>=2=사진선택→`OptionK.RecognizeImage`→`ProcessServerCamResult`(인식+정산)
  - `ProcessServerCamResult`: snapshot=null(수동TEST)이면 이미지저장/업로드 스킵
- **2026-06-19 11:51 개별설정 재실행 시 저장값 안 보이던 문제 수정(로드 순서 버그)**
  - 원인: `frmEnv_Load`가 213줄 `setEnv()`로 모든 컨트롤을 '전역값'으로 채우는데, 개별값 로드(`LoadServerCam`)는 `SetServerCamMode`에서 ShowDialog '전'(=Load 전)에 실행돼 **setEnv가 개별값을 덮어씀**. INI엔 저장되나 화면엔 전역값만 표시
  - 수정: 개별 적용 로직을 `ApplyServerCamConfig()`로 분리하고, **`frmEnv_Load` 끝(setEnv 이후)** 에서 호출. `SetServerCamMode`는 인덱스/제목만 설정(이미 로드됐으면 즉시 적용). LPR/카메라/차단기/전광판 모든 개별탭 표시값 복원
- **2026-06-19 11:39 개별설정 LPR 장비번호/채널/입출구 저장 안 되던 문제 수정**
  - 원인: `txtLPRNo1·txtEqpmNo1·CmbLPRInOut1·txtLPRName1`이 디자이너 기본 `Enabled=false`이고 `ChkLPRUse1`(LPR 사용) 체크 시에만 활성. 서버캠 개별설정에서 미체크면 **필드 비활성→입력 불가→저장될 값 없음**. (게이트 콤보 CmbGate1Port는 기본 활성이라 저장됐음 → 증상이 LPR만 안 됨)
  - 수정: `frmEnv.SetServerCamMode` 끝(LoadServerCam 이후)에서 `ChkLPRUse1.Checked=true` + `SetEnabled(true, txtLPRNo1/txtEqpmNo1/CmbLPRInOut1/txtLPRName1/CmbLPRType1)` 강제 활성. LoadServerCam 이후에 실행해 pc_값으로 재비활성 방지
  - `SaveServerCam`에 정산 핵심값(장비번호/채널/입출구/게이트포트) 저장확인 로그 추가
  - ⚠️ 재테스트: 카드 더블클릭→LPR설정→장비번호/채널/입출구 입력→저장. log에 "정산설정 저장 확인 — 장비번호=.. 채널=.. 입출구=.. 게이트포트=.." 찍히는지 확인
- **2026-06-19 11:11 C단계: 카드3~15 전체 정산동작(게이트/입출차/정기권)**
  - 배열 N대 확장: `clsDataTransaction.RegedCar[2]→[15]`, `clsFunction` `DioOutPut[2]→[15]`·`DisPlay[2]→[15]` (인덱스 0/1 불변, 2~14 기본값)
  - 신규 `Class/RegModule/clsServerCamEnv.cs`: [SVRCAM{n}] pc_*에서 카메라 2~14의 `Lpr_Info`(ChNo=pc_txtLPRNo1, EqpmNo=pc_txtEqpmNo1, InOutType=pc_CmbLPRInOut1, **LprOpt/SockInfo=cam1 템플릿 상속**)와 게이트(`DioOutPut[i]`=gate1 템플릿+포트 pc_CmbGate1Port) 구성→ENV 주입. GetLpr/GetInOut 캐시. csproj 등록
  - `DataProcess` LprInfo switch에 `default:`→`clsServerCamEnv.GetLpr(CamIdx)`, NetDev `CamIdx>=2`면 null(직렬 전광판 오출력 방지). `GateOpen`은 idx>=2면 기존에 이미 FullSpace 건너뛰고 RelayOn 직행(수정 불필요)
  - `frmLprMain.ProcessServerCamResult(camIdx,plate,snapshot,captureTime)`: 이미지저장({SavePath}\{date}\{ChName}_{plate}_{ts}.jpg + ParkingWeb 업로드) + `DataProcess.DataProcess(InOutType,ENV,camIdx,...)`(락 직렬화) + 카드표시. `InitServerModeUI`에서 `clsServerCamEnv.Load(camCount)`
  - `ServerCamManager.CaptureWork`: 인식 후 `clsThread.main.ProcessServerCamResult(index,plate,snap,capTime)` 호출(snap은 ProcessServerCamResult가 clone)
  - ⚠️ 미구현/주의: 카메라별 **네트워크 전광판** 출력(현재 서버캠 DisPlay.Use=false 무출력). 동시 캡처는 락으로 직렬화. [SVRCAM]에 장비번호/입출구/게이트포트가 개별설정 저장돼 있어야 정산 정확
- **2026-06-18 17:03** B단계: 카드3~15 캡처→ParkingWeb 인식→카드 차번표시. OptionK.RecognizeImage(public), CameraCard.GetSnapshot, ServerCamManager.Capture/CaptureWork(백그라운드, ROI 전체). 카드 캡처 ci>=2 → _serverCamMgr.Capture. **게이트/입출차 DB/전광판은 C단계 미구현**
- 2026-06-18 16:56 개별설정 장비설정 LPR2(2번 장비: txtEqpmNo2/txtLPRNo2/txtLPRName2/CmbLPRInOut2/cmbLPRPort2/CmbLPRType2/txtLPRProtocol2/ChkLPRUse2) 비활성 — 1대만
- 2026-06-18 16:54 개별설정 LPR설정 '장비 설정(gbLPR, 장비번호·입출구)' 활성화(perCam만) + PerCamRoots에 gbLPR 추가(카메라별 저장)
- 2026-06-18 16:40 A단계 보강: 개별설정 카메라IP를 카드1/2의 [CAMERA] cam1ip/cam2ip에도 반영(SaveServerCam). 원인=cam1ip=cam2ip=211(같은카메라)→카드2 충돌. 카드1/2는 서로 다른 실제 카메라 필요(한 카메라로 2카드 불가). 변경 후 재시작 필요
- 2026-06-18 16:28 A단계: ServerCamManager 생성(카드3~15 iNova2 연결·그랩→카드 영상). IP=[SVRCAM{n}]pc_txtCamIp. frmLprMain 배선(_serverCamMgr Start/Dispose). 영상만(캡처/인식 B단계)
- **2026-06-18 16:19** servermode/remote 분리 — '원격 차번인식 사용' 체크박스 복원([OPTIONK]remote, 업로드그룹), '서버모드' 라디오는 별도키 servermode. InitServerModeUI/ApplyServerModeUi가 servermode 사용. ⚠️재설정 필요(servermode 기본 꺼짐)
- 2026-06-18 16:11 개별필터 추가: 차단기 출력2(Gate2)·전광판 문구2(Display2) 비활성
- 2026-06-18 16:02 영역설정 오류수정: 개별모드에서 btnCam1/2 비활성→groupBox1.Text 불일치→frm null 크래시. _serverCamIndex로 카메라판정
- 2026-06-18 15:58 카드 표시이름 직접입력(txtCamCardName, 상단, perCam만)→[SVRCAM].name. 저장시 창닫힘 버그수정(DialogResult 제거). MaskedTextBox(시작/종료시간) 직렬화 추가
- 2026-06-18 15:51 개별설정 실제저장: 대상탭 입력컨트롤 [SVRCAM] pc_* 직렬화 저장/로드(WalkPerCam)
- 2026-06-18 15:47 폼크기 자동(BeginInvoke로 Load의 width=1000 덮어쓰기 이후 적용+화면클램프). camcount=5 정상저장 확인
- 2026-06-18 15:41 사용대수(camcount) 추가, 카드를 cam1/cam2서 생성, 서버캠 설정버튼 숨김
- 2026-06-18 15:21 카드 더블클릭→frmEnv 반대필터(개별설정), [SVRCAM] 별도저장
- 2026-06-18 15:0x 서버모드 환경설정 필터(공통항목만), 카드 더블클릭 이벤트
- 2026-06-18 14:55 인식 차번 카드 표시(UpdateServerCard via clsThread)
- 2026-06-18 14:43 캡처버튼 동작수정(PerformClick→핸들러직접호출). ParkingWeb 인식 성공 확인
- 2026-06-18 ~14:0x 카드 캡처버튼(열기/닫기/고정 제거), iNova 프레임 미러링(SetBitmap)
- 2026-06-18 카드그리드(CameraCard/ServerModePanel/InitServerModeUI), 서버모드 라디오, frmServerCams, ServerCamConfig
- 2026-06-16 OptionK 원격모드(ParkingWeb) 클라이언트, USB카메라 OptionK 디스패치 수정
